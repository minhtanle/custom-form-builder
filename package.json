{
  "name": "custom-form-builder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node tests/test-schema.mjs && node tests/test-sanitize.mjs && node tests/test-conditional.mjs",
    "dev": "vite --mode builder --open",
    "build": "npm run build:engine && npm run build:builder",
    "build:engine": "vite build",
    "build:builder": "vite build --mode builder",
    "prod": "npm run build && npm run preview:builder",
    "preview:builder": "vite preview --mode builder --open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@cfworker/json-schema": "^4.1.1",
    "preact": "^10.29.8",
    "sortablejs": "^1.15.7"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "terser": "^5.51.2",
    "vite": "^8.3.0"
  }
}
